<% if User.current.admin? || User.current.stealth_mode_permission? %>
  <fieldset class="box tabular">
    <legend><%= l(:stealth) %></legend>
    <p>
      <%= form.check_box(:stealth_allowed) %>
    </p>
  </fieldset>
<% end %>
